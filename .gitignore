/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/playwright-report
/storage/*.key
/storage/pail
/test-results
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
/.worktrees
/.claude/worktrees
/.playwright-mcp
/.delegate/briefs
/.superpowers

# Scratch review tooling. These drivers say in their own headers that they are
# one-off — they hardcode a single canvas and are meant to be deleted after its
# review — and `_shots/` is the several megabytes of PNGs they render. All of it
# regenerates on demand. `tools/canvas-serve.mjs` is deliberately not here: it
# serves any canvas in `docs/design/` and is tracked.
/docs/design/_shots/

# A canvas's seeded page: `seed-canvas.mjs` stamps the design skill's ~2 MiB
# payload with the artboards beside it, so it is a build product of the
# `.dc.html` sources and regenerates from them. One was committed by accident
# (3e297bf9, 11,009 lines) before this rule existed. The sources stay tracked.
/docs/design/*/*.html
!/docs/design/*/*.dc.html
/tools/canvas-verify.mjs
/tools/canvas-details.mjs
/tools/probe-*.mjs
